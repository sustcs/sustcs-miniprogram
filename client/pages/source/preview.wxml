<navigationBar isBack="{{true}}" bgColor="bg-gradual-blue">
    <view slot="backText"></view>
    <view slot="content">{{fileName}}</view>
</navigationBar>
<view class="cu-card no-card article">
    <view class="cu-item shadow">
        <view class="content">
            <wemark md="{{md}}" highlight type="wemark"></wemark>
        </view>
    </view>
</view>